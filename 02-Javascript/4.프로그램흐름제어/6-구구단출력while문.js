//5단출력
/* y가 1부터 10보다 작은 동안 1씩 증가하면서 반복=>반복범위 1~9
반복문이 수행되는 동안 y값에 5를 곱해 z값을 생성 */

let y =1;

while (y <=9) {
    const z = y * 5;
    console.log("5 X %d = %d", y, z);
    y++;
}

//12단 출력 해보기

let q = 1 ;
while ( q <=12 ){
    const w = q * 12 ;
    console.log("12 X %d = %d", q ,w);
    q++;
}